<!DOCTYPE html>
<html lang="en">
  <%- include('../includes/head'); %>
<body>
  
  <%- include('../includes/nav'); %>

  <div class="container mt-5">

    <h1>Login</h1>

    <% if (message.type) { %>
      <div class="alert alert-<%= message.type === 'success' ? 'success' : 'danger' %>" role="alert">
        <%= message.body %>
      </div>
    <% } %>

    <form method="POST" action="/auth/login">

      <div class="mb-3">
        <label for="email" class="form-label">Email address<sup class="text-danger">*</sup></label>
        <input 
        type="email" 
        class="form-control <%= errors.email ? 'is-invalid' : '' %>" 
        id="email" 
        name="email" 
        aria-describedby="emailHelp"
        value="<%= formData.email %>"
        maxlength="128"
        minlehgth="2"
        >
        <% if (errors.email) { %>
          <div class="invalid-feedback">
            <%= errors.email[0] %>
          </div>
        <% } %>
      </div>

      <div class="mb-3">
        <label for="password" class="form-label">Password<sup class="text-danger">*</sup></label>
        <input type="password" name="password" class="form-control <%= errors.password ? 'is-invalid' : '' %>" id="password" required>
        <% if (errors.password) { %>
          <div class="invalid-feedback">
            <%= errors.password[0] %>
          </div>
        <% } %>
      </div>

      <button type="submit" class="btn btn-primary">Submit</button>
    </form>

    <div class="btn-group" role="group" aria-label="Basic example"></div>
      <a href="/auth/google" class="btn btn-primary google"><i class="bi bi-google"></i></a>
      <button type="button" class="btn btn-primary"><i class="bi bi-facebook"></i></button>
      <button type="button" class="btn btn-primary"><i class="bi bi-apple"></i></button>
    </div>

</div>

  <%- include('../includes/scripts'); %>

</body>
</html>